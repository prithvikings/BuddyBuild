# Dependencies
node_modules/

# Environment variables
.env
# You can also use **/.env to ignore .env files in any subdirectory
# Or you can specify paths if you have multiple .env files (e.g., client/.env, server/.env)

# Build artifacts
build/
dist/

# Logs and temporary files
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pids
*.pid
*.seed
lib-cov
coverage

# OS and IDE files
.DS_Store
.idea/
.vscode/
.lock-wscript

# Optional: client-specific build files if they are named differently
# client/build/ 
# client/dist/

# Optional: server-specific build files if they are named differently
# server/build/
# server/dist/